PLUGIN_DIR="$HOME/.config/sketchybar/plugins"

sketchybar --bar \
color=0xff212121 \
height=30 \

sketchybar -m --add event window_focus \
              --add event title_change

sketchybar --default \
label.font=Menlo:Bold:16 \
label.padding_right=4 \
label.padding_left=4 \

sketchybar --add item time right \
           --set time script="$PLUGIN_DIR/time.sh" \
           --set time click_script="$PLUGIN_DIR/time_clicked.sh" \
           --set time label.color=0xffcccccc update_freq=10

sketchybar --add item date right \
           --set date script="$PLUGIN_DIR/date.sh" \
           --set date click_script="$PLUGIN_DIR/date_clicked.sh" \
           --set date label.color=0xffc9c9c9 update_freq=600

sketchybar --add item weather right \
           --set weather script="$PLUGIN_DIR/weather.sh" \
           --set weather click_script="sketchybar -m --set \$NAME popup.drawing=toggle" \
           --set weather label.color=0xffffcc66 update_freq=600

sketchybar --add item radar popup.weather \
           --set radar script="$PLUGIN_DIR/radar.sh" \
           --set radar label.color=0xffffcc66 update_freq=600

sketchybar --add item window center \
           --set window script="$PLUGIN_DIR/window.sh" \
           --set window click_script="$PLUGIN_DIR/window_clicked.sh" \
           --subscribe window window_focus front_app_switched space_change title_change \
           --set window label.color=0xfff2777a

sketchybar --add space space1 left \
           --set space1 associated_space=1 \
           --set space1 script="$PLUGIN_DIR/space.sh" \
           --set space1 click_script="$PLUGIN_DIR/space_clicked.sh" \
           --set space1 label="main" \
           --subscribe space1 window_focus front_app_switched space_change title_change \
           --set space1 label.color=0xff99cc99 \
           --set space1 background.color=0xff99cc99 \
           --set space1 label.highlight_color=0xff212121

sketchybar --clone space2 space1
sketchybar --set space2 associated_space=2 label="sub" label.color="0xffffcc66" background.color="0xffffcc66"
sketchybar --clone space3 space1
sketchybar --set space3 associated_space=3 label="comms" label.color="0xff6699cc" background.color="0xff6699cc" 
sketchybar --clone space4 space1
sketchybar --set space4 associated_space=4 label="rss" label.color="0xffcc99cc" background.color="0xffcc99cc"
sketchybar --clone space5 space1
sketchybar --set space5 associated_space=5 label="ha" label.color="0xff66cccc" background.color="0xff66cccc"

sketchybar --update

