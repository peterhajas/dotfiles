# Palette defaults and host overrides.
# Family palettes live in colors/.config/colors/palettes/*.toml

default_variant = "modus_operandi"
default_family = "modus"

# Optional per-host overrides. Example:
# [hosts.my-hostname]
# default_variant = "modus_vivendi"
[hosts]
